import React, { useEffect } from 'react';
import { useSelector, useDispatch } from 'react-redux';

import MainLayout from '../layouts/MainLayout';

const FilterSession = props => {
    
    useEffect(() => {
       
    }, []);

    return (
        <MainLayout>
            <div>
                filter
            </div>
        </MainLayout>
    );
}

export default FilterSession;